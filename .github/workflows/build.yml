name: Build RMK firmware
on:
  workflow_dispatch:
  push:
    paths:
      - "firmware/**"

jobs:
  build:
    uses: haobogu/rmk/.github/workflows/user_build.yml@main
    with:
      keyboard_toml_path: "firmware/keyboard.toml"
      vial_json_path: "firmware/vial.json"